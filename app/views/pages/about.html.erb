<% provide(:title, "About") %>

<% Category.all.each do |category| %>
	<li class="<%= 'active' if params[:category] == category.name %>">
		<%= link_to category.name, listings_path(category: category.name), class: "link" %>
	</li>
<% end %>
